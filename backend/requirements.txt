# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.41.0
pydantic>=2.12.0
pydantic-settings>=2.12.0
python-dotenv>=1.2.0

# Database
sqlalchemy>=2.0.48
alembic>=1.18.0
psycopg2-binary>=2.9.9

# HTTP client
httpx>=0.28.0
requests>=2.32.0

# ML model loading & inference
xgboost>=3.1.0
lightgbm>=4.6.0
scikit-learn>=1.7.0
joblib>=1.5.0
pandas>=2.3.0
numpy>=2.3.0
# pyarrow: required by pandas for Parquet I/O (pd.read_parquet).
# prediction_service.py reads ml/data/processed/club_matches.parquet at inference time.
pyarrow>=14.0.0

# ML training & tuning
optuna>=4.7.0
mlflow>=3.10.0
shap>=0.50.0
# MLflow resolves model artifacts from gs:// URIs (our prod registry backend is
# GCS). Without google-cloud-storage, every predict call fails with
# "No module named 'google.cloud'" — silently skipping every match.
google-cloud-storage>=2.18.0

# Background scheduling
APScheduler>=3.11.0

# Data ingestion
kagglehub>=1.0.0
